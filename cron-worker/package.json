{
  "name": "intinor-cron-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker: fires the dashboard's /api/cron/poll every minute via a Cron Trigger. See README.md §Phase 25 in the repo root.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled --config wrangler.toml",
    "deploy": "wrangler deploy --config wrangler.toml",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "typescript": "^5",
    "wrangler": "^4"
  }
}
