import { PhasePlaceholder } from "@/components/PhasePlaceholder";

export default function EncodersPage() {
  return (
    <PhasePlaceholder
      title="Encoders"
      description="Push-output configuration (SRT/RTMP/RIST destinations, encoding modes, adaptive bitrate) and live output monitoring."
    />
  );
}
