import { PhasePlaceholder } from "@/components/PhasePlaceholder";

export default function MixerPage() {
  return (
    <PhasePlaceholder
      title="Video mixer"
      description="Layered compositing with x/y/zoom per layer — layout presets like a 2×2 quad grid will live here."
    />
  );
}
